# build output
dist/
# generated types
.astro/
# deps
node_modules/
# copied at build from node_modules
public/fonts/
# os / editor
.DS_Store
*.log
.vercel/
.netlify/

# Pages deploy workflow — kept local until the gh token has the `workflow`
# scope (`gh auth refresh -h github.com -s workflow`), otherwise pushes are
# rejected. Remove this line to commit it. See DEVELOPING.md.
.github/workflows/
